#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <assert.h>
#include <string.h>

// void mergeLists(Node* head,Node *L1, Node *L2);
// void splitLists(Node* start, Node *L1, Node *L2);
// void merge(Node *L1, Node *L2);

void merge(int* A, int *B);
void mergeSort(int* A, int* B, int* C);

void merge(int* A, int *B)
{
	int Aptr = 0; 
}

int main()
{
	return 0;
}